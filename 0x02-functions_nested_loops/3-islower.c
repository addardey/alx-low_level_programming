#include "main.h"
/**
 * main - check the code
 *
 * _islower -> checks if the character is lower case
 *
 * @c:  character augment
 *
 * Return: Always 0
 */
int main('_islower'(int c))
{
	return (c >= 'a' && c <= 'z');
}
